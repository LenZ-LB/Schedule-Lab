/* config.js — loaded before shared.js on every page.
   Leave SCHEDULE_API_URL empty to keep using the static JSON files in
   docs/data/ (the current, default behavior). Once the Fly API is deployed,
   set this to its URL (e.g. "https://schedule-lab-api.fly.dev") and every
   page picks it up automatically — no other file needs to change. */
window.SCHEDULE_API_URL = "";
